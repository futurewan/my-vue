import regEmpty from './regEmpty.filter'
import date from './date.filter'
import gbk from './gbk.filter'
import numberFormat from './numberFormat.filter'
import preZero from './preZero.filter'
import privacy from './privacy.filter'
import week from './week.filter'
export default {
  regEmpty: regEmpty,
  date: date,
  gbk: gbk,
  numberFormat: numberFormat,
  preZero: preZero,
  week: week,
  privacy: privacy
}
